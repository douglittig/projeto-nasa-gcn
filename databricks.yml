# This is a Databricks asset bundle definition for nasa_gcn.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: nasa_gcn
  uuid: 5319b056-d74a-46ce-b392-88f283579cd2

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-5fa38af7-00ad.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-5fa38af7-00ad.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/dltreinamentos.data@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/dltreinamentos.data@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: dltreinamentos.data@gmail.com
        level: CAN_MANAGE
